# Cached Incus documentation

- `manifest.json` lists every cached page and its upstream URL.
- `incus-quick-reference.md` contains project-specific operating guidance.
- `pages/` contains the fetched documentation pages as Markdown text.

Refresh with `python3 ../scripts/update-docs.py` from this skill directory.
